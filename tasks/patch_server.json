{
  "input_method": "environment",
  "parameters": {
    "reboot": {
      "description": "Should the server reboot after patching has been applied?",
      "type": "Boolean"
    },
    "security_only": {
      "description": "Limit patches to those tagged as security related? (Defaults to false)",
      "type": "Optional[Boolean]"
    }
  },
  "puppet_task_version": 1,
  "supports_noop": false,
  "description": "Carry out OS patching on the server, optionally including a reboot"
}
